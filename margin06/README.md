<<<<<<< HEAD
# margin06 — Pair Trading: BTC LONG + ETH SHORT

## Стратегия

Парная торговля на двух коррелированных инструментах.

Одновременно открываются **два ордера** на одинаковую сумму:
- **LONG** BTC-USDT-SWAP
- **SHORT** ETH-USDT-SWAP

## Логика работы

```
Цикл:
  1. Проверяем позиции по BTC и ETH
  2. Если обе открыты → управляем трейлинг-стопами
  3. Если одна закрылась → ждём закрытия второй
  4. Если обе закрыты → открываем новую пару
```

## TP / SL

| Параметр | Значение |
|---|---|
| TP активация | 2 × ATR от цены входа |
| SL активация | 1 × ATR от цены входа |
| TP callback | `callback_atr_multiplier × ATR / цена` |
| SL callback | `callback_sl_atr_multiplier × ATR / цена` |

ATR считается независимо для BTC и ETH. Ордера закрываются независимо.

## Запуск

```powershell
.\trade.ps1              # dryRun режим
.\trade.ps1 -ForceLive   # боевой режим
.\trade.ps1 -DebugMode   # debug вывод
```

## ⚠️ Важно

- Не коммить `config_60m.json` с реальными ключами
- Если BTC открылся, а ETH нет — закрой BTC вручную
=======
# margin06 — Pair Trading: BTC LONG + ETH SHORT\n\n## Стратегия\n\nПарная торговля на двух коррелированных инструментах.\n\nОдновременно открываются **два ордера** на одинаковую сумму:\n- **LONG** BTC-USDT-SWAP\n- **SHORT** ETH-USDT-SWAP\n\nСтавка на опережение BTC относительно ETH. Встроенный хедж снижает зависимость от общего направления рынка.\n\n## Логика работы\n\n```\nЦикл:\n  1. Проверяем позиции по BTC и ETH\n  2. Если обе открыты → управляем трейлинг-стопами\n  3. Если одна закрылась → ждём закрытия второй\n  4. Если обе закрыты → открываем новую пару\n```\n\n## TP / SL\n\n| Параметр | Значение |\n|---|---|\n| TP активация | 2 × ATR от цены входа |\n| SL активация | 1 × ATR от цены входа |\n| TP callback | `callback_atr_multiplier × ATR / цена` |\n| SL callback | `callback_sl_atr_multiplier × ATR / цена` |\n\nATR считается **независимо** для BTC и ETH.\nОрдера закрываются **независимо**.\n\n## Запуск\n\n```powershell\n# Тестовый режим\n.\\trade.ps1\n\n# Боевой режим\n.\\trade.ps1 -ForceLive\n\n# Debug\n.\\trade.ps1 -DebugMode\n```\n\n## ⚠️ Важно\n\n1. Вставь API ключи в `config_60m.json` перед запуском\n2. Если BTC открылся, а ETH нет — закрой BTC вручную\n3. Не коммить `config_60m.json` с реальными ключами\n
>>>>>>> e3fbd593a4b91166762a1978c13e1387f93d06a3
